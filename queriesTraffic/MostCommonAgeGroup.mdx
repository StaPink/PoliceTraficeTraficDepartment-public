-- What types of tickets are most commonly issued? 

SELECT {
	[Measures].[Amount]
} 
ON COLUMNS,
ORDER(
    {[Criminal].[Age Category].Members},
    [Measures].[Amount],
    DESC
)
ON ROWS
FROM [Traffic Department DW cube]